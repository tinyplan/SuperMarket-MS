<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.software.demo.dao.GoodsMapper">

    <resultMap id="goodsMapping" type="Goods">
        <id column="goods_id" jdbcType="VARCHAR" property="id"/>
        <result column="goods_name" jdbcType="VARCHAR" property="name"/>
        <result column="goods_type" jdbcType="VARCHAR" property="type"/>
        <result column="goods_cost" jdbcType="DECIMAL" property="cost"/>
        <result column="goods_price" jdbcType="DECIMAL" property="price"/>
        <result column="production_date" jdbcType="VARCHAR" property="productionDate"/>
        <result column="duration" jdbcType="INTEGER" property="duration"/>
        <result column="expiration_date" jdbcType="VARCHAR" property="expirationDate"/>
    </resultMap>

    <resultMap id="goodsDOMapping" type="com.software.demo.entity.domain.GoodsDO">
        <id column="goods_id" jdbcType="VARCHAR" property="id"/>
        <result column="goods_name" jdbcType="VARCHAR" property="name"/>
        <result column="goods_type" jdbcType="VARCHAR" property="type"/>
        <result column="goods_cost" jdbcType="DECIMAL" property="cost"/>
        <result column="goods_price" jdbcType="DECIMAL" property="price"/>
        <result column="production_date" jdbcType="VARCHAR" property="productionDate"/>
        <result column="duration" jdbcType="INTEGER" property="duration"/>
        <result column="expiration_date" jdbcType="VARCHAR" property="expirationDate"/>
        <result column="stock" jdbcType="INTEGER" property="stock"/>
    </resultMap>

    <insert id="insertGoods" parameterType="Goods">
        INSERT INTO goods_info (goods_id, goods_name, goods_type, goods_cost, goods_price, production_date, duration,
                                expiration_date)
        VALUES (#{id}, #{name}, #{type}, #{cost}, #{price}, #{productionDate}, #{duration}, #{expirationDate})
    </insert>

    <delete id="deleteGoods" parameterType="String">
        delete
        from goods_info
        where goods_id = #{id}
    </delete>

    <update id="updateGoods" parameterType="Goods">
        UPDATE goods_info
        SET goods_cost      = #{cost},
            goods_price     = #{price},
            production_date = #{productionDate},
            duration        = #{duration},
            expiration_date = #{expirationDate}
        WHERE goods_id = #{id}
    </update>

    <select id="queryAllGoods" resultMap="goodsMapping">
        select goods_id,
               goods_name,
               goods_type,
               goods_cost,
               goods_price,
               production_date,
               duration,
               expiration_date
        from goods_info
    </select>

    <select id="queryGoods" resultMap="goodsDOMapping">
        select gi.goods_id as goods_id,
               goods_name,
               goods_type,
               goods_cost,
               goods_price,
               production_date,
               duration,
               expiration_date,
               stock
        from goods_info as gi
        left join import as i on gi.goods_id = i.goods_id
        limit #{start}, #{limit}
    </select>


</mapper>